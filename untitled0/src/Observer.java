// Create the Observer interface to define the update method that observers will implement.
public interface Observer {
    void update(String message); // Method to update the observer with a message.
}
